# Wake word models

`hey_ned.onnx` goes here. It is trained, not downloaded: see `docs/wakeword.md`. Models are
committed to the repo (they are small, a few hundred KB) so the Pi gets them with `git pull`.
